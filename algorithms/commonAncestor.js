// Common ancestor in two tree nodes. 
// Given an array of objects that represent a binary tree (each object in the format {parent: child}), construct the tree and find out if two given nodes share an ancestor.
